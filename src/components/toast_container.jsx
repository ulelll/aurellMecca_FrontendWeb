import { ToastContainer as ReactToastContainer } from 'react-toastify';

const CustomToastContainer = () => <ReactToastContainer position="top-right" autoClose={3000} />;

export default CustomToastContainer;